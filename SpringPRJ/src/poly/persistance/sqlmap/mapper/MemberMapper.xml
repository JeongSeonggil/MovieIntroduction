<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="poly.persistance.mapper.IMemberMapper">

	<!-- 유저 등록 -->
	<insert id="insertMember" parameterType="MemberDTO">
		INSERT INTO myAwsDB.member
		(
		    EMAIL,
		    MEMBER_PW,
			MEMBER_NAME,
		    MEMBER_NIC
		)
		VALUES (
				#{EMAIL},
				#{MEMBER_PW},
				#{MEMBER_NAME},
				#{MEMBER_NIC}
				)



	</insert>

	<!-- 로그인 -->
	<select id="loginMember" parameterType="MemberDTO" resultType="MemberDTO">
		SELECT
		       MEMBER_NAME,
		       MEMBER_NIC
		FROM
		       myAwsDB.member
		WHERE  EMAIL = #{EMAIL} AND MEMBER_PW = #{MEMBER_PW}
	</select>
</mapper>