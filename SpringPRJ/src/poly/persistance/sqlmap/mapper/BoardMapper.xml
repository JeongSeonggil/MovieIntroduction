<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="poly.persistance.mapper.IBoardMapper">

	<!-- 게시판 리스트 조회 -->
	<select id="selectBoard" resultType="BoardDTO">
	SELECT  notice_seq,
	       	title,
	       	user_nic,
	       	read_cnt,
	       	reg_dt
	       FROM myAwsDB.board
	</select>

	<!-- 글 등록 -->
	<insert id="newPost" parameterType="BoardDTO">
		INSERT INTO myAwsDB.board
			(
			 title,
			 contents,
			 user_nic
			)
			VALUES
			(
				#{TITLE},
				#{CONTENTS},
				#{USER_NIC}
			)


	</insert>
</mapper>